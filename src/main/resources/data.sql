CREATE TABLE IF NOT EXISTS employee (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    address VARCHAR(255),
    dob VARCHAR(10),
    phone VARCHAR(15),
    title VARCHAR(255),
    sin VARCHAR(4)
);